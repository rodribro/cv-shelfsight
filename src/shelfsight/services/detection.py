class DetectionService:
    def __init__(self, model):
        self.model = model

    def create_run(self, image):
        # Placeholder for creating a new run in the database
        run_id = 12345
        detections = {}
        total_detections = 0
        products = {}
        return run_id, detections, total_detections, products
