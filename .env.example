# App
APP_NAME=ShelfSight
LOG_LEVEL=INFO

# Database (Docker Compose)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/shelfsight

# AWS (use IAM roles on EC2 ideally; keep empty locally)
AWS_REGION=eu-west-1
S3_BUCKET=your-bucket-name
S3_ENDPOINT_URL=